[build]
builder = "NIXPACKS"
buildCommand = "echo 'Pre-built — no build step needed'"

[deploy]
startCommand = "node deploy/index.mjs"
healthcheckPath = "/api/healthz"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
