[build]
builder = "NIXPACKS"
buildCommand = "echo 'pre-built'"

[deploy]
startCommand = "node deploy/index.mjs"
healthcheckPath = "/api/healthz"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
